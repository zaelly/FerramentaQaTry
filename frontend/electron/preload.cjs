// Reserved for future secure IPC bridges between the renderer and the
// Electron main process. Nothing is exposed today; the renderer talks to
// the local Python backend directly over HTTP/WebSocket.
